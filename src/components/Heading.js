import React from "react"
import styled from "styled-components"
const Heading = () => {
return (

        <Headline>Daniel's Project Portfolio</Headline>
);
}

export default Heading

const Headline = styled.h1`
    color: #61dafb;
`